VITE_API_BASE_URL=http://localhost:5000/api

# Auth is served in the browser (src/api/authMock.js) until the backend
# implements /auth. Set to false once the Express API is running.
VITE_USE_MOCK_AUTH=true
