# 5000 conflicts with macOS's AirPlay Receiver — the backend runs on 5001.
VITE_API_BASE_URL=http://localhost:5001/api

# The Express API implements /auth for real now. Set to true only to develop
# the UI with no backend running at all (src/api/authMock.js takes over).
VITE_USE_MOCK_AUTH=false
